DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="30d"
PORT=4000

# Optional — free tier at https://aistudio.google.com/apikey. Without
# this, OCR classification and syllabus pacing fall back to the existing
# heuristics/math, which already work on their own.
GEMINI_API_KEY=
